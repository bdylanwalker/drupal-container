; Drupal-specific PHP-FPM pool overrides.
; This file is loaded last (zzz-drupal.conf) and overrides the default www.conf.

[www]
; Pass all container environment variables through to PHP.
; Without this, env vars set in the Container App are invisible to PHP-FPM workers.
clear_env = no